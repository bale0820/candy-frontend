## 🔧 환경 변수 설정 (Environment Variables)

이 프로젝트는 API 주소 및 이미지 경로를 환경 변수로 관리합니다.

### 1️⃣ `.env.local` 파일 생성

프로젝트 루트에 있는 `.env.example` 파일을 복사하여  
개인 로컬 환경 설정 파일을 생성합니다.

```bash
cp .env.example .env.local